{
  "version": 2,
  "buildCommand": "cd ../client && GOOS=js GOARCH=wasm go build -o ../static/typing-magic.wasm .",
  "outputDirectory": "static",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ]
}

